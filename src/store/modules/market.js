import * as types from '@/store/mutation-types'
const state = {
   numInfo:[1,2,3,4]
}
const getters = {
  numInfo: state => state.numInfo,
}
const actions = {

}
const mutations = {

}
export default {
  state,
  getters,
  actions,
mutations
}